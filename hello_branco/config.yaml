name: "Hello Branco"
version: "1.1.9"
slug: "hello_branco"
description: "Test add-on"

startup: application   # 🔥 IMPORTANT CHANGE
boot: auto

init: false            # disable s6

arch:
  - aarch64
  
devices:
  - /dev/i2c-1

privileged:
  - SYS_RAWIO

options: {}
schema: {}
